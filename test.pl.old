# Before `make install' is performed this script should be runnable with
# `make test'. After `make install' it should work as `perl test.pl'

######################### We start with some black magic to print on failure.

# Change 1..1 below to 1..last_test_to_print .
# (It may become useful if the test is moved to ./t subdirectory.)

BEGIN { $| = 1; print "1..1\n"; }
END {print "not ok 1\n" unless $loaded;}
use PDL;
use PDL::Char;
use McIDAS;

$loaded = 1;
print "ok 1\n";

######################### End of black magic.

# Insert your test code below (better if it prints "ok 13"
# (correspondingly "not ok 13") depending on the success of chunk 13
# of the test code):

#
# Convert date, lat, lon to sun fixed lat/lon
#

$a=zeroes(long,3000,9);
set($a,0,0,3);
set($a,0,1,3);
set($a,0,2,3);
set($a,0,3,3);

$ar=PDL::Char->new(zeroes(byte,256,1));
$ar->setstr(0,'GRID0101');
#$ar->setstr(1,'YAESTA');
@dar=dims($ar);
print "dims ar @dar\n";

$b=zeroes(long,3000,9);
@db=dims($b);
print "dims b @db\n";
$n=pdl(long,0);
@dn=dims($n);
print "dims n @dn\n";

#print $a->xchg(0,1)->slice(':,0');
print $b->xchg(0,1)->slice(':,0');
#print $b->slice('0,:');

h_grdlist($b,$n,$ar);
#print $a->xchg(0,1)->slice(':,0');
print $b->xchg(0,1)->slice(':,0');
print $n,"\n";
#print $b->slice('0,:');



McIDAS::grdlist('GRID0101');





